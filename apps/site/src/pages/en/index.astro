---
import Base from '../../layouts/Base.astro'
import { LandingApp } from '../../components/LandingApp'
import { t } from '../../i18n'

const copy = t('en')
---

<Base title={copy.meta.title} description={copy.meta.description} lang="en">
  <LandingApp client:load initialLocale="en" />
</Base>
